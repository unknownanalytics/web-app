<%= form_with(model: dashboard_view, local: true) do |form| %>
  <% if dashboard_view.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(dashboard_view.errors.count, "error") %> prohibited this dashboard_view from being saved:</h2>

      <ul>
      <% dashboard_view.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
