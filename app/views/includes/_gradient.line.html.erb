<svg xmlns="http://www.w3.org/2000/svg"
     id="geo_gradient"
     width="<%= width ||= 500 %>" height="10" viewBox="0 0 <%= width %> 10">
  <defs>
    <!-- https://stackoverflow.com/questions/21638169/svg-line-with-gradient-stroke-wont-display-straight/34687362-->
    <linearGradient id="gradient" gradientUnits="userSpaceOnUse">
      <% colors.each_with_index do |color, index| %>
        <stop offset="<%= (index.to_f / (colors.length - 1).to_f) * 100 %>%" stop-color="<%= color %>"/>
      <% end %>
    </linearGradient>
  </defs>
  <line x1="0" y1="0" y2="0" x2="100%" stroke="url(#gradient)" stroke-width="10" fill="none"/>
</svg>